- hosts: windows
  tasks:
  
  - name: Create directory (YAML format works)
    win_file:
      path: C:\test\tmp
      state: directory
      
  - name: Create directory (YAML format works)  
    win_file:
      path: C:\test\tmp1
      state: directory